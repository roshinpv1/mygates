# CodeGates Environment Configuration
# Copy this file to .env and uncomment/configure the values you need

# SECRET_KEY (Optional - auto-generated if not provided)
# Recommended for production to maintain session persistence across restarts
# SECRET_KEY=your-secure-random-secret-key-here

# Container-friendly Directory Configuration (Optional)
# Set these if running in containers with restricted file system access
# TEMP_REPO_DIR=/app/temp
# UPLOAD_FOLDER=/app/uploads
# REPORTS_DIR=/app/reports

# Git Repository Checkout Configuration (Optional)
# Configure fallback behavior when git clone fails
# CODEGATES_PREFER_API_CHECKOUT=false       # Prefer GitHub API over git clone
# CODEGATES_ENABLE_API_FALLBACK=true        # Enable automatic fallback to API
# CODEGATES_GIT_TIMEOUT=300                 # Git clone timeout in seconds
# CODEGATES_API_TIMEOUT=120                 # API download timeout in seconds
# CODEGATES_MAX_REPO_SIZE=100               # Maximum repository size in MB

# LLM Integration (Optional - for enhanced analysis)
# Uncomment and set one of these to enable LLM-enhanced scanning:

# OpenAI API Key
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key  
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# GitHub Token (Optional - for private repositories)
# GITHUB_TOKEN=ghp_your-github-token-here

# Container Environment Variables (for OpenShift/Kubernetes)
# TMPDIR=/app/temp                     # Override system temp directory
# HOME=/app                           # Override home directory
# CODEGATES_CONTAINER_MODE=true       # Enable container-optimized mode 